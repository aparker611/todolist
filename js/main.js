$(document).ready(function() {
  var $submit = $('.submit');
  var $input  = $('.addit');
  var $itemContainer = $('.item');

  $itemContainer.hide();

  function inputLength(input) {
    return $input.value.length();
  }

  function returnInputLength(input) {
    return $input.value.length();
  }

  function addItem(input){
  }
});
